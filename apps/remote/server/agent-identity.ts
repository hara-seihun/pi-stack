/**
 * How the agent presents itself in transcripts, notifications and Meet. The
 * app is Kenan on every screen, so the transcript names the agent the same way
 * instead of the generic role.
 */
export const AGENT_NAME = "Kenan";
/** Kenan's head, served from the web app's static root. */
export const AGENT_AVATAR = "kenan.png";
